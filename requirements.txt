# Install these into PoleScan's OWN Python environment (the Anaconda
# environment that already has torch, transformers, arcgis, numpy,
# pymupdf, and pillow -- see PoleScan's INSTALL.txt). Do NOT install
# PoleScan's dependencies here; this service imports PoleScan's code
# in-process and needs to run inside that same environment, not a
# separate one.

fastapi~=0.115
uvicorn[standard]~=0.32
python-multipart~=0.0.12
