fastapi~=0.115
uvicorn[standard]~=0.32
python-multipart~=0.0.12
python-dotenv~=1.0
arcgis~=2.4

# Pole discovery (app/discovery/)
pymupdf~=1.28.0
pillow~=10.4
transformers
accelerate
safetensors

# torch is deliberately left unpinned here -- install the CUDA-matched
# build for your GPU/driver from https://pytorch.org/get-started/locally/
# BEFORE running `pip install -r requirements.txt`, or a plain `pip
# install torch` may pull a CPU-only wheel.
torch
